name = "atxp-api"
main = "worker.js"
compatibility_date = "2024-11-01"

# Bind a KV namespace for stored keys. Create it with:
#   npx wrangler kv namespace create ATXP_KV
# then paste the returned id here.
kv_namespaces = [
  { binding = "ATXP_KV", id = "REPLACE_WITH_KV_ID" }
]
